{
  "name": "bolao",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "dev:web": "turbo run dev --filter=@bolao/web",
    "dev:api": "turbo run dev --filter=@bolao/api",
    "build:web": "turbo run build --filter=@bolao/web"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "packageManager": "pnpm@9.15.0"
}
